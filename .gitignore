# Ignore everything inside the uploads directory
# Except for the .keep file, so that git will keep the empty directory
uploads/**
!uploads/.keep
jobs/**
!jobs/.keep

tensors/**
!tensors/.keep

# Cache
__pycache__/

# Environment variables
.env